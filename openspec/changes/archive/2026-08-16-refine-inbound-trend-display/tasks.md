## 1. 入库周期规则

- [x] 1.1 为入库趋势增加可注入业务日期的周期构造测试，覆盖 2026-08-01 的全月、截止1日、1W–5W 和日列1–7顺序
- [x] 1.2 增加固定月内七日分桶构造逻辑，确保平年二月只有1W–4W、闰年二月有单日5W、任何月份都不生成6W
- [x] 1.3 增加月末当前周测试并实现当周全部自然日日列，包含周六周日且不跨月
- [x] 1.4 让入库紧凑视图和展开视图共用同一周期集合，移除展开视图额外整月日列

## 2. 截止当天与表格聚合

- [x] 2.1 扩展入库周期取数逻辑，使全月聚合全部记录、截止当天聚合1日至当天且包含当天、周列聚合完整周段、日列聚合单日
- [x] 2.2 在入库 loader 中接入截止当天列及动态月份/日期表头，同时保持现有部门过滤、接口请求和月级缓存逻辑不变
- [x] 2.3 增加全月未来计划、截止1日、截止16日、周段和表格真实零值的聚合测试

## 3. 入库图表有效点策略

- [x] 3.1 增加入库专用图表数据投影测试，覆盖累计列排除、零值转 `null`、非有限值和空值处理
- [x] 3.2 实现入库图表首尾空区间裁剪，并保留第一个与最后一个有效点之间的空类目及等长 series 数据
- [x] 3.3 从图表数据中移除完全无有效点的 series，并验证由有数据更新为空时不残留旧曲线和图例
- [x] 3.4 为入库折线显式配置 `connectNulls: false`，增加 `100 → 0 → 120` 不跨零值连线的配置与数据测试
- [x] 3.5 增加所有图表系列均为空或为零时的空图表状态测试

## 4. 回归验证与项目记忆

- [x] 4.1 运行入库趋势、生产计划趋势、出勤率趋势和 `TableChartCard` 相关测试，确认入库专用策略未改变其他推移表行为
- [x] 4.2 运行 `npm run type-check` 并修复本变更引入的类型问题
- [x] 4.3 同步 `doc/factory-dashboard-real-data-mapping.md` 与 `doc/factory-dimensions.md` 的入库周期和图表规则
- [x] 4.4 若实现调整共享月分段入口，同步 `doc/shared-utilities.md`；更新 `doc/agent-memory-map.md` 使新 capability 和实现入口可被后续智能体定位
