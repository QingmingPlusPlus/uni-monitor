## 1. 接口分类与数据模型

- [x] 1.1 核对一条线上新人出勤记录，确认其为 `positionType=direct` 且 `positionName` 包含“新人”，并将该响应形态固化为测试样例
- [x] 1.2 在人员出勤行模型和 mock 模板中增加 `directNewcomer`，为所有明细行与合计行提供确定数值
- [x] 1.3 在真实出勤聚合中计算新人数量、从正式工兜底中排除新人，并在工序及部门合计中累加新人

## 2. 表格展示

- [x] 2.1 在人员出勤表紧凑态和展开态中增加“新人”表头及数据单元格，按“顶岗、新人、合计”顺序展示
- [x] 2.2 将表格网格扩展为 14 列，并同步调整直接分组、直接在籍、实际出勤表头跨度及末行边框规则
- [x] 2.3 在部门维度和工序维度检查常规卡片与展开弹窗的表头、数据列对齐和新增列可读性
- [x] 2.4 为组长、正式工、派遣工、临时工和新人表头增加固定类别色，保持班长、顶岗和数据单元格的中性底色
- [x] 2.5 在部门与工序维度检查紧凑态和展开态的岗位表头配色一致性
- [x] 2.6 提高五个岗位末级表头的色彩饱和度，并确认班长、顶岗、其他分组表头和数据单元格保持原有颜色

## 3. 自动化验证

- [x] 3.1 增加新人分类测试，验证新人只计入新人列且不会重复计入正式工
- [x] 3.2 增加工序合计和部门全体合计测试，验证新人累加正确且直接在籍、实际出勤和出勤率保持原口径
- [x] 3.3 运行相关 Vitest、TypeScript 类型检查和项目构建，修复本变更引入的失败

## 4. 项目记忆与验收

- [x] 4.1 更新真实接口字段映射和接口缺口文档，记录新人分类已接入及仍采用 `positionName` 关键词的约束
- [x] 4.2 将人员出勤表入口补充到 `doc/agent-memory-map.md`，先指向当前 change spec，并注明归档后切换为长期 spec 路径
- [x] 4.3 对照 `personnel-attendance-table` spec 逐项确认紧凑态、展开态、零新人和多班次/多工序场景
