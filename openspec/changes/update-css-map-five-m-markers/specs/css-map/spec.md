## ADDED Requirements

### Requirement: 图例说明全部 5M 变化点

地图图例必须（MUST）在现有负荷率和工况图例下方显示“颜色 / 5M变化点”分组，并按人、机、料、法、环顺序展示全部五类菱形色样和文字说明。图例内容超过地图可用高度时必须（MUST）允许在图例内部纵向滚动，且不得遮挡或改变地图操作控件的交互。

#### Scenario: 查看完整 5M 图例

- **WHEN** 用户打开普通地图或展开地图
- **THEN** 负荷率和工况图例下方显示 5M 变化点图例
- **AND** 图例按人、机、料、法、环顺序展示与设备标记一致的菱形色样

#### Scenario: 地图高度不足以容纳完整图例

- **WHEN** 三组图例的总高度超过图例可用区域
- **THEN** 图例容器提供纵向滚动以访问全部 5M 类型
- **AND** 地图平移、缩放、聚焦和点选控件仍可正常操作

## MODIFIED Requirements

### Requirement: 人员和 5M 标记语义保持不变

人员必须（MUST）继续使用现有班次扇形标记。5M 变化点必须（MUST）使用带正向“人/机/料/法/环”glyph 的填充菱形标记，并使用固定类别配色：人=紫色、机=红色、料=绿色、法=近黑色、环=黄色；文字颜色必须（MUST）与填充色保持清晰对比。纵向布局最多使用三列两行共六个槽位，横向布局最多使用五个单行槽位；内容超过槽位容量时，最后一个槽位必须（MUST）显示中性 `+N` 汇总，不得将 `+N` 误绘制为任一类别色的菱形。

#### Scenario: 显示五类变化点

- **WHEN** 同一设备具有“人、机、料、法、环”五类 5M 变化点
- **THEN** 每个变化点显示对应类别颜色的填充菱形
- **AND** 菱形内显示正向且居中的对应 glyph
- **AND** 五类标记的形状、颜色和文字足以彼此区分

#### Scenario: 标记数量超过布局容量

- **WHEN** 人员或 5M 标记数量超过当前方向的槽位容量
- **THEN** 保留容量减一数量的原有标记
- **AND** 最后一个槽位显示未单独绘制的总数 `+N`
- **AND** `+N` 使用中性汇总样式

### Requirement: 默认和回退渲染器一致

WebGL Sprite 默认渲染器与 CSS3D DOM 回退渲染器必须（MUST）使用同一方向判断、内容宽度档位、标记容量和信息顺序；两者还必须（MUST）使用同一 5M 类别配色、菱形几何语义、正向 glyph 和文字对比规则。

#### Scenario: WebGL 初始化失败

- **WHEN** 页面从 Sprite 地图回退到 CSS3D 地图
- **THEN** 同一设备的方向、信息宽度和留白行为保持一致
- **AND** 同一 5M 变化点的类别颜色、菱形形状和 glyph 语义保持一致
- **AND** 不改变设备数据、路由或交互契约
