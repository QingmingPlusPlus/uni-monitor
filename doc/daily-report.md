# 制造日报

## 当前实现与评估（2026-09-22）

2026-09-22已按用户提供的后端说明接入 `getRejects.yuanyin`（现象）、`number`（现象数量）及可选查询参数 `date`。日报生产、产线与品质传 `month+date`，计划/实绩仍取整月供历史归属关联；日不良共享键包含日期。仅 `type=不良` 进入不良分子和现象，同制番同名现象合计并按数量降序排列，页面显示前两项，现象不良率以主体含废弃实绩为分母。其它类型仍参与既有废弃、实绩、合格公式。缺现象时已知现象标为部分，不伪造名称或计算部分比率。

在线Swagger认证读取成功，但getRejects仍只声明month，尚未补录date/yuanyin；上述新增契约依据用户提供的后端说明。只读查询 `month=2026-09`、`month=2026-09&date=2026-09-21`、`month=2026-07&date=2026-07-01` 均success=true且data=[]，不能据此宣称已验证非空现象或服务端日期过滤效果。以下9月21日查询结果保留为历史依据。

独立 H5 页面为 `src/pages/daily-report/index.vue`，路由 `/pages/daily-report/index`，部门、工序看板顶部提供入口。页面只读查询，历史查询反映服务端当前数据，不包含填报、审批、归档、导出、打印或发布快照。

**日报已改为调用现有真实接口，不再请求未发布的 `/daily-report/*`。生产区按用户确认的制番、设备即产线及数量公式展示。** 洗净/粘接只是示意，不要求新增分类；计划停止时间暂不显示，可动率和占比显示接口原值。品质区已同步相同数量公式和不良空值规则，现象已接入yuanyin/number，仍缺模具关联；出勤保持现有口径。逐项覆盖情况见[日报字段真实数据汇总](daily-report-data-coverage.md)。

| 分区 | 已接入来源 | 当前展示与缺口 |
| --- | --- | --- |
| 出勤实绩 | `/attendance/twoDayAttendancePerformance` | 已恢复，两个查询均返回3行。出勤及六类原因可展示，分类支持0.5、1.5等小数；班长为数据日整体名单，不分班次。尚缺班次完成状态、起止时刻，分类单位和合计差异需确认。 |
| 生产实绩 | `/schedule/getPlan`、`getOutput`、`getRejects` | 按制番合计；流动取实绩接口，废弃取全部不良记录，实绩=流动+废弃，合格=流动+非不良。成功无不良记录按0。 |
| 设备生产与阻碍 | 月计划/实绩＋日不良＋`/device/availability/day/report` | 已恢复，三个查询返回35/59/56台设备，但时长及可动率全部为0、阻碍原因全空。设备即产线；数量采用与上表相同的公式，时间和比例按返回值展示。 |
| 品质实绩 | 月实绩＋日不良，月计划辅助确认归属，设备日报补充产线名称 | 按制番展示与生产相同的实绩、合格及不良数；不良率=不良/含废弃实绩；成功空不良按0，全零时保留明细、不排名；现象取type=不良的yuanyin/number。 |

本次 `2026-09` 月计划5775条、实绩0条；`2026-07` 月计划1105条、实绩9条，实绩仅覆盖制造3课后处理的7月1日和22日。`getRejects` 的5、7、9月均为空，生产与品质按确认的业务约定视为不良及废弃为0。出勤和设备接口不再使用9月14日的超时结论；设备结构已取得非空响应，但非零时长、阻碍项及比率尺度仍未验证。详细请求依据见 [接口参考](api-reference.md)。

本次修复：缺勤分类不再因小数被隐藏；schedule兼容Swagger明确示例的十进制数量字符串；单条异常记录隔离，有效部分可显示但不参与比率或排行；空月份、筛选无匹配、日期/制番缺失、设备重复及全零均有明确提示。

## 查询与范围

| URL 参数 | 说明 |
| --- | --- |
| `departmentId` | `department1` 至 `department4`；无效值使用选择配置默认部门。 |
| `processType` | `preprocessing`、`sulfur_addition`、`post_processing`，限部门支持的工序族；无效值回退首个工序族。 |
| `date` | `YYYY-MM-DD` 数据日，默认设备本地昨日；非法值回退并提示。报告日固定次日。 |
| `from`、`sourceProcessId` | 保留部门/工序入口与返回目标；筛选不再匹配原工序时返回新工序族首项。 |

修改部门或工序自动查询四区并更新 URL；单改日期通过“查询”提交。未提交条件不改变已显示日报标签，且禁止刷新；“刷新”重新读取已应用条件。选择配置失败时使用内置配置并提示。

日报读取完整月计划/实绩及所选日不良，按记录日期、部门和工序族过滤，**不读取地图设备显示白名单、不借用当前实时设备归属**。制造1课的前处理1、前处理2合并查询一次，区别于看板生产性卡片各自设备范围。

工序归并：`前处理/前处理1/前处理2 → preprocessing`，`加硫 → sulfur_addition`，`后处理/仕上检查/出货检查包装 → post_processing`，同时接受 API 工序枚举。检查和包装归后处理由用户在本次任务中确认。未知工序或缺少部门的同日记录排除并提示，相关已知合计标为部分，不能参与比率与排行。

制造2课月记录也包含仕上检查，因此日报额外提供该部门的后处理选项。此选项不修改地图工序配置；从工序看板返回时，若没有对应地图工序，返回部门看板。

`getRejects` 未提供部门、工序时，仅通过该月计划/实绩中同一设备的唯一部门＋工序归属关联；冲突、未知归属不强行分配，显式字段不能被设备关联覆盖。未关联不良会提示并阻止不完整合计参与排名。日期归属使用服务端 `date/workDate`，不编造跨午夜起止时刻。

## 模块与接口

- `src/api/dailyReport.ts`：前端显示模型及原始请求薄封装，统一 `/api` 前缀、15秒超时、AbortSignal；这些类型不是服务端已发布的新契约。
- `reportSources.ts`：原始来源成功/结构检查，以及进行中请求去重。三区共享同月计划/实绩及同日不良，不缓存已完成结果。单区取消不影响其他订阅者，最后一个取消才中断上游。
- `reportAdapters.ts`：工序归并、历史范围过滤、指标完整性、生产/设备/制番聚合及源字段映射。
- `reportApi.ts`：将真实来源装配为四区显示模型；部分来源失败保留其他可用字段，全部必要来源失败才使整区报错。
- `reportResource.ts`、`reportValidation.ts`：四区独立状态、范围/结构校验、取消与版本保护；卸载不再发布结果。
- `reportModel.ts`、各 Section：日期、比率、排名及显示。样式从脚本导入 `dailyReport.css`，限定 `.daily-report`。

| 原始请求函数 | 参数 |
| --- | --- |
| `getReportAttendanceSource` | `dataDate`、次日 `reportDate`、`department`、`processType` |
| `getReportPlanSource/getReportOutputSource` | `month=YYYY-MM` |
| `getReportRejectsSource` | `{ month: YYYY-MM, date: YYYY-MM-DD }`；通用类型允许省略date查询整月 |
| `getReportDeviceSource` | `day`、`departmentId`、`processType` |

原始响应必须 `success=true`，不能仅凭 HTTP200 或某一种成功码判断。整个响应结构异常、业务失败、超时分别提示；个别非对象记录隔离，保留有效记录并标记合计不完整，全部记录无效则来源报错。真实零保留0；生产与品质的不良接口成功且范围完整时，空数组或分组无记录按0；计划、实绩接口无记录仍保留缺失。日报不导入 mock，测试样本只在测试文件中使用。

## 显示模型与口径

`ReportMetric` 为 `{ value: number | null, status: complete | partial | unavailable, note? }`。缺失或无效数字显示 `— / 未提供`；有效部分值可查看但不参与比率。人数、生产数量、次数只接受非负安全整数，时间和原始比例接受非负有限数。缺勤六类统计保留非负有限小数，页面最多两位小数；不擅自解释成人数或天数。仅schedule数量按Swagger示例显式兼容十进制字符串（可带零小数部分）；空字符串、null、十六进制、非整数及超安全整数不能变成有效数量。

`ReportMeta` 保留查询范围、次日报告日、完整性与说明。现有源没有统计起止时间和数据更新时间，因此 `periodStart/periodEnd=null`，`timestampSource=retrieved`，`updatedAt` 仅表示实际读取时间。显示采用工厂时区 `Asia/Shanghai`；不能将读取时间标成服务端更新时间。各区显示模型均保留完整性；出勤区按参考图隐藏状态徽标、说明和时间元信息；生产汇总及排行也隐藏来源说明、归属日及读取时间。

### 出勤

`statDate/shiftType/shiftName` 对应日期与班次；按两日端点契约，报告日行属于报告日早班。没有稳定起止时间，不通过中文名称猜测时间；`status=reported`、`startAt/endAt=null` 保留在显示模型中，页面不显示状态或起止时间。

`onRollCount/actualAttendanceCount/absenceCount` 对应直接排班、出勤和服务端缺勤；`attendanceRate` 按文档百分数显示一位小数。六类原因依次来自 `annualLeaveCount/nursingLeaveCount/sickLeaveCount/personalLeaveCount/otherLeaveCount/absenteeismCount`，保留小数；按用户要求，出勤区域不显示出勤超排班或缺勤分类差异的黄色说明条，差异校验仍用于完整性判定。状态未知时不做排班减出勤、不重新计算出勤率。`monitorNames` 每日只显示一次，名单不分班次、不加到直接人员人数；名单缺失不影响有效出勤行。日期/班次异常及重复班次排除并保留内部说明，展示其他有效班次，全部无效则报错。


#### 出勤区域参考图展示（2026-09-20）

正常加载后仅显示 `1. 出勤实绩`、`1-1. 出勤班长`、`1-2. 直接人员出勤` 和表格。移除图外的口径说明、读取时间、时区、统计范围、状态徽标、重新读取按钮、班次状态、异常说明及“未提供”附注；请求失败时仍提供错误与重试，不能将失败包装成正常空表。

表格以“班次”为组合表头，包含日期、星期、班次；同日日期/星期合并单元格。日期显示 `YY年MM月DD日`，星期由日期计算。视觉复用日报共用ReportSection卡片、report-table表格样式和MetricValue数值组件；标题、字号、间距、圆角、表头、边框及颜色使用项目设计变量，取消原型的硬编码灰底标题和黑色网格。表格全宽对齐，数据日主统计区使用surface、报告日使用operation-soft、正数缺勤使用danger色；出勤率不根据图片中的单个红色示例猜测告警阈值。六类原因的真实0按图留白，非零含小数正常显示；未知/无效数值保留“—”，不与真实0混淆。MetricValue新增可选plain（不显示附注）及blankZero（真实零留白），默认行为不变；班长名单为空时保留标题、姓名区留白；不复制图片中的示例姓名。

本次对制造3课后处理重新读取：2026-09-19的 `monitorNames=[]`、3条rows，各统计字段均存在且有数值；2026-07-01的monitorNames有1人、3条rows。图中所需业务字段已全部有来源，无必须新增的字段：班长monitorNames，日期statDate，班次shiftName/shiftType，在籍onRollCount，实绩actualAttendanceCount，出勤率attendanceRate，缺勤absenceCount，分类annualLeaveCount/nursingLeaveCount/sickLeaveCount/personalLeaveCount/otherLeaveCount/absenteeismCount。

未显示原因必须区分：当前班长空白是接口返回空数组；分类空白是明确返回0后按图隐藏，不是字段缺失；班次状态/起止时刻、名单班次归属、源更新时间和完整性标记确实没有字段，但不属于参考图要求。分类小数单位和出勤率标红阈值属于待确认口径，不是假装缺了某个已有业务列。

### 生产与品质

生产按 `zhifan` 分组，取所选日期、部门、工序内计划/实绩/不良的制番并集并排序，相同制番跨设备和班次合计。缺制番记录不编造名称，相关合计标为部分并保留内部说明。计划取 `ΣgetPlan.number`；流动取 `ΣgetOutput.number`；不良取 `type=不良`；废弃取全部 `getRejects.number`；合格=流动+明确非不良数量；实绩=流动+废弃。达成率=实绩/计划，合格率=合格/实绩。生产与品质共用同一数量聚合，品质不良率分母也为含废弃的实绩数。

生产与品质的不良来源成功且范围完整时，空数组、当前分组无记录或某分类无记录按0。废弃不限制类型白名单；非空且不等于“不良”的类型均属于非不良。类型缺失可计入有效废弃总数，但不良/非不良分类为缺失或部分。源失败、无效数量、日期/归属不明不能补0；计划/实绩无记录仍为缺失。任一加法输入缺失则结果缺失，部分输入只形成部分小计，不参与比率与排行。

品质按 `zhifan` 分组实绩和已确认不良，`meta.qualityDimension=production_number` 明示降级维度。无模具关联时仍按真实制番维度展示，现象取type=不良的yuanyin，按同制番同名现象合计number；按数量降序、并列按名称排序，仅显示前两项。缺失名称不能将总数分摊给已知现象；相关小计标部分，缺项显示“—”。完整分子分母存在时计算达成率、不良率；计划或实绩分母须大于零。生产只排达成率低于90%的最低3条产线（设备）；品质只排实绩>0、不良>0的最高3项，并列按稳定编码。缺少制番的记录会触发提示并阻止相关合计参与排行。品质无法排名时在相同表格中保留真实明细但名次显示“—”，有排名时仅显示最高三项；生产区域仅展示有效排行。

### 设备时间与原因

`day/report` 必须匹配请求日期、部门和工序；重复设备统计不能重复合计。`totalRunHours/productionHours/obstructionHours` 转成显示模型秒，页面显示小时两位小数。`obstructionItems` 用 `pauseType/pauseTypeName/count/obstructionHours`，按时长排序，生产区仅显示前三项。

设备即产线，直接以 `shebei/deviceCode` 关联，名称取 `deviceName`，缺名称回退编码。产线统计不要求额外映射、不合并不同设备时长。生产区已移除计划停止列；显示模型的 `plannedStopSeconds/availableSeconds` 暂保留兼容字段，不用于展示或计算。

`availabilityRate/obstructionItems[].ratio` 直接显示数值，不换算、不取整、不追加百分号；例如0.845显示0.845，84.5显示84.5，0显示0。缺失显示“—”。单位和分母确认不作为接入前置条件。

## 待后端补齐与验证

| 缺口 | 实测依据与影响 | 后端需要提供/确认 |
| --- | --- | --- |
| 当前月生产实绩 | 9月实绩为空，无法计算达成率及品质分母；7月历史实绩可以展示 | 目标数据日的完整实绩记录，含日期、部门、工序、设备、制番、数量 |
| 不良及废弃 | 按既有number/type契约接入；生产与品质成功空数组按0 | 非空实测不是实施前置条件 |
| 品质现象和模具排行 | yuanyin/number已按后端说明接入，尚无非空实测；无模具关联 | 非空现象样本、模具—制番关联 |
| 设备时间及原因 | 三个样本全部为0、原因数组全空 | 确认采集是否完成，提供非零历史样本、暂停次数和时长及去重规则 |
| 生产时间与比例展示 | 设备即产线，比例显示原值，计划停止列略过 | 比例单位、生产线映射和计划停止时间不作为本次实施条件 |
| 出勤异常 | 9月19日制造1课夜班在籍0、实绩29；分类出现0.5/1.5且部分合计与缺勤总数不一致 | 核对排班统计、分类折算单位和合计规则；提供班次完成状态、起止时间 |
| 数据完整性 | 无源更新时间、统计起止和完整性标记 | 区分未采集、部分、已完成及真实零；提供服务端更新时间 |

出勤和设备查询已恢复，无需继续沿用“接口超时”作为当前缺口。`getShijiByDate` 的Swagger仍标记未完成（本次未重测业务响应），未接入日报；`getWorkhours` 为图形类型，也不是设备运转时间。

## 验证入口

2026-09-22验证：日报50项及不良API参数1项通过，全仓库304项通过，类型检查与H5构建通过。新增覆盖yuanyin跨设备班次合计、其它类型排除、范围过滤、现象比率、缺失名称/非法数量、日请求参数和不同日期的共享取消隔离。只读后端查询成功但不良仍为空；非空现象通过测试样本验证，未将样本写入业务数据，未宣称已完成真实非空页面核验。

2026-09-21品质接入后验证：日报46项、全仓库299项测试通过，类型检查和H5构建通过。制造3课后处理2026-07-01真实页面显示5个制番：实绩与合格数一致，不良0、不良率0.00%，关联产线显示设备名称；右侧现象三列仍为“—”。1920px和390px检查无页面横向溢出，品质表局部滚动，浏览器error日志为空。测试覆盖含废弃分母改变排名、正不良率前三及并列、全零不排名、产线名称缺失/重复/错误范围/失败回退、共用请求和取消。

2026-09-21本次验证：日报43项、全仓库296项测试通过，类型检查与H5构建通过。真实代理页面制造3课后处理2026-07-01显示47条制番明细；不良与废弃空数据为0，已有流动数量可生成实绩、合格和合格率。产线排行仅CG涂装-6符合条件，计划1,947、实绩360、达成率18.5%、可动率原值0。已确认红字说明及计划停止列移除；1920px和390px页面无整体横向溢出，窄屏汇总及排行分别局部横向滚动，浏览器error日志为空。原值小数及非空不良公式通过测试样本验证，未写入业务数据。

2026-09-20验证：日报29项、全仓库282项测试通过，类型检查和H5构建通过。真实代理页面制造3课后处理（2026-07-01）四区加载成功：计划49,858、实绩13,542、达成率27.16%，制番明细与出勤分类1.5可见；制造1课前处理（2026-09-19）也加载四区，修复时已验证分类0.5、出勤统计差异、所选日无匹配计划、9月实绩/不良为空及设备时长全零提示，浏览器未捕获error日志。

- `npm test -- src/pages/daily-report`：日期、筛选、公式、排名、契约校验、真实源映射、未知工序、空数据/失败、共享取消与旧响应保护。
- `npm run type-check`、`npm run build:h5`：类型与 H5 构建。
- 浏览器核对真实历史日期的数量、失败来源提示、明细切换和筛选恢复；宽屏及窄屏检查字号、局部横向滚动。测试数据不得进入业务配置。

### 生产实绩制番与产线展示（2026-09-21）

上方为九列表格：制番、计划数、实绩数、合格数、流动数、不良数、废弃数、达成率、合格率。下方为纵向No.1–No.3产线表，每项左侧四列数量及四列时间/可动率，右侧为阻碍项目/时间/次数/占比。移除计划停止列，设备使用生产线列名；保留项目表格、数值组件、设计变量和单元格告警色。

“前日”“含不良”“达成率最差生产线（90%以下）”均为红字说明，不显示。低于90%的排名规则仍生效，使用含废弃的实绩；不足三项不补位。正常说明、元信息和展开入口不显示；汇总与排行仍独立加载和失败重试，后者通过ReportSection的after插槽保留，ReportSection支持无标题且不生成空标题区域。

2026-09-21只读核验：9月计划5775条、实绩0条、不良0条；7月计划1105条、实绩9条、不良0条。7月1日制造3课后处理设备日报返回59台，时间和可动率全0，阻碍数组为空。在线Swagger返回401，声明参考9月20日快照；按现有契约开发，不将不良非空样本作为阻碍。品质日报现已同步生产的实绩分母及空不良按0规则。

### 品质实绩展示与真实数量接入（2026-09-21）

品质区域复用ReportSection、MetricValue及项目表格/设计变量，改为一张九列表格：名次、制番（有权威模具维度时为模具）、实绩数、合格数、不良数、不良率、不良现象、不良数、不良率。每个主体固定两行现象，前六列跨行合并；主体名称下显示关联产线名称，通过同日、同部门/工序的唯一deviceCode关联deviceName，缺失、重复或名称源失败时回退设备编码。第一现象使用项目浅黄色单元格底色，数值与文字保持普通文本色。

不显示原型中的红字“个数不良率最高制番”“加硫按照模具”，也不显示说明副标题、状态徽标、读取时间、范围、缺口说明、全部明细、展开现象、重新读取和卡片外的口径文案；保留图中单位。失败/加载仍使用共用异常状态。

完整实绩和不良数按不良率降序取前三项并显示No.1–No.3，不足三项不伪造条目。没有可排名记录（包括不良全零）时，在同一表格保留已有记录，名次为“—”；不良为0且实绩有效时显示0.00%，不生成零值排行；空数据保留表头与“—”。每项现象只展示前两项，缺项显示“—”，不填图中的0或0.00%。

品质取实绩/不良制番并集，相同制番跨设备、班次合计；不新增只有计划的品质行。实绩=ΣgetOutput.number+全部废弃，合格=ΣgetOutput.number+明确非不良，不良仅type=不良，与生产共用productionQuantities；成功无不良按0。缺失制番、异常数量、归属不明或源失败仍保留缺失/部分，不参与比率和排行。

getDailyQuality并行读取月源和设备日报，名称源与产线区共享进行中请求及取消引用计数。设备接口仅补充名称，失败不阻止有效数量展示；实绩和不良源均失败时品质区仍报错。现有九列及两行现象布局不变，不新增说明条；加载/失败重试保留。

2026-09-22现象已读取type=不良的yuanyin及number，重复名称合计；空现象列表仍显示“—”，type不能冒充现象。模具关联未提供，加硫继续按制番。现象不良率以同一主体的含废弃实绩为分母。完整数据覆盖汇总见[日报字段真实数据汇总](daily-report-data-coverage.md)。
