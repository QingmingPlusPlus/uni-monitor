# 日报表格真实数据覆盖汇总

更新：2026-09-21。以现有日报界面、已核对接口契约及本会话真实查询为依据；不将测试样本当作业务数据，不把空数组与接口缺字段混为一谈。

## 核验范围

- 页面复核：制造3课、后处理、2026-07-01；生产47条制番明细、1条有效产线排行，品质5条制番明细。
- 本会话只读请求：2026-07计划1105条、实绩9条、不良空数组；2026-09计划5775条、实绩和不良均空数组。这些为整月条数，不等于页面筛选后的行数。
- 2026-07-01制造3课后处理设备日报59台，时间及可动率均为0，阻碍列表均为空。
- 在线Swagger本次返回401；已声明字段参考仓库2026-09-20快照。按现有契约实施，不良非空实测不作为前置条件。

## 当前表格覆盖情况

| 区域 | 字段 | 接口来源或计算 | 当前真实数据情况 | 页面处理 |
| --- | --- | --- | --- | --- |
| 出勤 | 班长、日期、班次、在籍、实绩出勤、出勤率、缺勤及六类原因 | twoDayAttendancePerformance的monitorNames及rows | 已有真实来源和非空样本，包含0及小数分类 | 按返回值显示；分类0留白，缺失为“—” |
| 生产 | 制番、计划数、流动数 | getPlan/getOutput的zhifan、number | 已有真实数据；所选日期可能只有计划或只有实绩；9月实绩为空 | 按制番并集合计，不匹配的计划/流动显示“—” |
| 生产、品质 | 不良数、废弃数（废弃仅生产显示） | getRejects.number/type | 字段已声明，7月和9月实际返回空数组 | 成功且范围完整时按0；不属于字段缺失；失败或归属不明不转0 |
| 生产、品质 | 实绩数、合格数 | 流动+全部废弃；流动+明确非不良 | 已接入真实来源计算；7月有流动的制番可显示；9月流动缺失则结果缺失 | 依赖来源完整才计算，缺失为“—” |
| 生产、品质 | 达成率/合格率（生产）、不良率（品质） | 实绩/计划、合格/实绩、不良/实绩 | 可计算；当前所选日不同制番的计划和实绩不一定匹配 | 分母缺失或为0、不完整时“—”；有效实绩且不良为0显示0.00% |
| 产线、品质 | 产线名称 | shebei关联同日同范围唯一deviceCode/deviceName | 已有真实名称，设备即产线 | 缺名称、重复或名称来源失败时回退编码 |
| 产线 | 总运转时间、生产时间、阻碍时间、可动率 | totalRunHours/productionHours/obstructionHours/availabilityRate | **字段存在，设备样本实际全部返回0** | 时间显示0.00，可动率显示原值0；不是缺字段，也不自行判断采集完整性 |
| 产线阻碍明细 | 阻碍项目、阻碍时间、次数、占比 | obstructionItems的pauseTypeName/obstructionHours/count/ratio | **字段存在，当前列表为空，暂无可显示明细** | 明细显示“—”，不补示例原因；有数据时比例原值展示 |
| 品质 | 制番、实绩数、合格数、不良数、不良率 | 与生产共用公式，按实绩/不良制番并集汇总 | 已有真实显示：5个制番实绩及合格数相等，不良0、不良率0.00% | 正常展示，无需新增合格数字段 |
| 品质现象明细 | 不良现象 | **现有契约未提供现象编码/名称** | **真正缺字段**；type只区分不良/非不良 | 显示“—”，不能用type或设备阻碍项目代替 |
| 品质现象明细 | 现象对应不良数 | **缺少按现象分类的数量及与制番的关联** | **真正缺分类数据**；不良总数不能分配到具体现象 | 显示“—”，不能把总不良重复填入每种现象 |
| 品质现象明细 | 现象对应不良率 | 现象不良数/同制番含废弃实绩 | **缺分子**；无需单独新增百分比字段 | 现象数量补齐后可计算，目前“—” |
| 生产、品质 | No.排名 | 达成率低于90%的最低三条产线；正不良率最高三个制番 | 当前生产仅1条符合条件；品质不良全0，没有正不良率记录 | 生产仅No.1；品质保留明细、排名为“—”；不是缺排名字段 |

生产与品质在同一范围、同一制番下共用实绩、合格、不良公式。品质不含只有计划的制番。所有数值仍保留缺失/部分状态，接口失败不伪装为0。

## 不属于当前表格待补字段

- 计划停止时间：用户已要求略过，界面已移除，不计入当前表格缺口。
- 洗净/粘接分类：仅为原型示意，已使用制番，无需新增分类。
- 生产线映射：设备即产线，已有编码与名称，无需新增映射。
- 模具：当前包括加硫在内均显示制番；若以后要求模具排行，再补模具编号及与生产/不良记录的关联。
- 可动率和阻碍占比单位：按用户要求直接展示原值，不是本次待确认项。

## 后端最小补充项

若要填满品质右侧三个现象字段，需要提供同一统计范围内的**制番、不良现象标识/名称、该现象不良数量**；若仍以设备记录归属范围，则保留日期、设备等关联字段。现象不良率可由前端按主体实绩计算，不必另增比率字段。具体字段名待后端发布，前端不猜测未声明的别名。

实现及测试入口见[制造日报](daily-report.md)。
